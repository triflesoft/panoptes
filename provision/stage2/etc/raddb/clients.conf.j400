client localhost_strongswan {
    ipaddr   = 127.0.0.1
    proto    = *
    secret   = {{ password }}
    nas_type = other

    limit {
        max_connections = 16
        lifetime = 0
        idle_timeout = 30
    }
}
